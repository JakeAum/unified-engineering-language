"""The conformance suite — the artifact all kernel work is graded against.

Discipline (program §2.2): cases are authored from the spec's text, not from the
implementation's behavior. A case that encodes an implementation quirk is a defect
in the suite. The suite runs with zero third-party dependencies.
"""
