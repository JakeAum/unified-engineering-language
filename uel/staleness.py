"""Staleness reporting against the lock (Phase 3 — placeholder until then)."""

from __future__ import annotations

from .diagnostics import Bag
from .resolver import Resolution


def report(res: Resolution, bag: Bag) -> None:  # Phase 3
    return
