"""Envelope compatibility checking (Phase 4 — placeholder until then)."""

from __future__ import annotations

from .diagnostics import Bag
from .resolver import Resolution


def check(res: Resolution, bag: Bag) -> None:  # Phase 4
    return
